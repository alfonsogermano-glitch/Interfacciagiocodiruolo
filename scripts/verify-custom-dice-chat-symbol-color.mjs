import assert from 'node:assert/strict';
import fs from 'node:fs';
function read(path){return fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8')}
const faceResult=read('src/app/components/session/dice/CustomDieFaceResult.tsx');const historyCard=read('src/app/components/session/dice/DiceRollHistoryCard.tsx');const libraryIcon=read('src/app/components/session/dice/CustomDieLibraryIcon.tsx');const engine=read('src/app/components/session/dice/diceEngine.ts');const types=read('src/app/components/session/dice/diceTypes.ts');
assert.ok(faceResult.includes('symbolColor?: string;')&&faceResult.includes("stroke={symbolColor || 'currentColor'}"),'Custom die icon results must use the saved symbol stroke');
assert.ok(faceResult.includes('bodyColor?: string;')&&faceResult.includes('backgroundColor: bodyColor')&&faceResult.includes('data-custom-die-face-image'),'Custom chat results must render transparent face images over the saved body color');
assert.ok(faceResult.includes('skinId?: DiceSkinId;')&&faceResult.includes('getDiceSkinBackgroundImage'),'Custom chat result surfaces must render the saved static skin');
assert.ok(/symbolColor=\{die\.customFace\.symbolColor\s*\?\?\s*group\.customDieSnapshot\?\.symbolColor\}/.test(historyCard)&&/bodyColor=\{group\.customDieSnapshot\?\.bodyColor\}/.test(historyCard)&&/skinId=\{group\.customDieSnapshot\?\.skinId\s*\?\?\s*'none'\}/.test(historyCard),'Dice history must pass custom symbol/body/skin snapshots to the face renderer');
assert.ok(types.includes('export interface RollCustomDieFace extends CustomDieFace')&&types.includes('symbolColor?: string;'),'Rolled custom faces must persist their icon color through realtime serialization');
assert.ok(engine.includes("face.visual.kind === 'icon' ? { symbolColor } : {}")&&engine.includes('snapshotRolledCustomFace(tensFace, die.symbolColor)')&&engine.includes('snapshotRolledCustomFace(unitsFace, die.symbolColor)')&&engine.includes('snapshotRolledCustomFace(customFace, die.symbolColor)')&&engine.includes('snapshotRolledCustomFace(customFace, customItem.customDie.symbolColor)'),'Every rolled custom icon face, including d100/explosions, must carry symbol color while images remain untinted');
for(const [name,source] of [['chat',faceResult],['library',libraryIcon]]){const images=[...source.matchAll(/<img\b[^>]*>/g)].map((m)=>m[0]);assert.ok(images.length>0,`${name} must render raw Custom face images`);for(const image of images){assert.ok(!/filter\s*:|symbolColor|skinId/.test(image),`${name} image faces must never receive tint/filter/skin styles`)}}
assert.ok(libraryIcon.includes('data-custom-die-image-untinted')&&faceResult.includes('data-custom-die-image-untinted'),'untinted Custom image branches must expose regression hooks');
console.log('Custom dice chat symbol color verification passed.');
