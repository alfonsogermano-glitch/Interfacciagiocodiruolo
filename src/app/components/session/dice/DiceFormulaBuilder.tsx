import { Plus } from 'lucide-react';
import { DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuTrigger } from '../../ui/dropdown-menu';
import { DiceFormulaRow } from './DiceFormulaRow';
import type { DiceFormulaItem,SavedCustomDie } from './diceTypes.ts';
function createItem(kind:Exclude<DiceFormulaItem['kind'],'custom-die'>):DiceFormulaItem{const id=globalThis.crypto.randomUUID();switch(kind){case'compare':return{id,kind,operator:'gte',target:1,total:false};case'dice':return{id,kind,sides:20,quantity:1};case'drop':return{id,kind,which:'highest',count:1};case'exploding':return{id,kind,mode:'explode'};case'keep':return{id,kind,which:'highest',count:1};case'modifier':return{id,kind,operation:'add',value:1}}}
export function DiceFormulaBuilder({items,itemErrors,onChange,customDice=[]}:{items:DiceFormulaItem[];itemErrors:Record<string,string[]>;onChange:(items:DiceFormulaItem[])=>void;customDice?:SavedCustomDie[]}){const replaceItem=(next:DiceFormulaItem)=>onChange(items.map(item=>item.id===next.id?next:item));const removeItem=(id:string)=>onChange(items.filter(item=>item.id!==id));const moveBy=(index:number,delta:-1|1)=>{const target=index+delta;if(target<0||target>=items.length)return;const next=[...items];[next[index],next[target]]=[next[target],next[index]];onChange(next)};const moveDragged=(draggedId:string,targetId:string,position:'before'|'after')=>{const from=items.findIndex(i=>i.id===draggedId);const target=items.findIndex(i=>i.id===targetId);if(from<0||target<0||from===target)return;const next=[...items];const[dragged]=next.splice(from,1);const targetIndex=next.findIndex(i=>i.id===targetId);const insertionIndex=position === 'after' ? targetIndex + 1 : targetIndex;next.splice(insertionIndex,0,dragged);onChange(next)};const append=(kind:Exclude<DiceFormulaItem['kind'],'custom-die'>)=>onChange([...items,createItem(kind)]);return <div className="space-y-2.5">{items.map((item,index)=><DiceFormulaRow key={item.id} item={item} customDice={customDice} errors={itemErrors[item.id]} canMoveUp={index>0} canMoveDown={index<items.length-1} onChange={replaceItem} onRemove={()=>removeItem(item.id)} onMoveUp={()=>moveBy(index,-1)} onMoveDown={()=>moveBy(index,1)} onDropItem={moveDragged}/>)}<div className="flex justify-center py-2"><DropdownMenu><DropdownMenuTrigger asChild><button type="button" data-dice-modifier-add aria-label="Aggiungi elemento alla formula" className="flex h-9 w-9 items-center justify-center rounded-full border border-[var(--dash-border)] bg-[var(--dash-surface)] text-[var(--dash-muted)]"><Plus className="h-4 w-4"/></button></DropdownMenuTrigger><DropdownMenuContent align="center" className="z-[1000] min-w-48 !bg-[var(--dash-panel)] !border-[var(--dash-border)] !text-[var(--dash-text)]"><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('compare')}>Confronto</DropdownMenuItem><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('dice')}>Dado</DropdownMenuItem><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('drop')}>Scarta</DropdownMenuItem><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('exploding')}>Esplosione</DropdownMenuItem><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('keep')}>Mantieni</DropdownMenuItem><DropdownMenuItem className="text-[var(--dash-text)] focus:!bg-[var(--dash-surface-2)] focus:!text-[var(--dash-text-strong)]" onClick={()=>append('modifier')}>Modificatore</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></div>}
