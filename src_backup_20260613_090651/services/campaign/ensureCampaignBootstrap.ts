import { CAMPAIGN_STORAGE_KEYS } from './campaignStorageKeys';
import { DEFAULT_CAMPAIGN_ID } from '../../config/campaign.config';

export function ensureCampaignBootstrap() {
  // Bootstrap rimosso: nessun dato di default viene più creato.
  // Gli utenti devono creare manualmente avventure e luoghi secondo necessità.
}